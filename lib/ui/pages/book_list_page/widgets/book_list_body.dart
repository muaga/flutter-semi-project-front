import 'package:flutter/material.dart';

class BookListViewBody extends StatelessWidget {
  const BookListViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView();
  }
}
